using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Mrak : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

        ScoringManager.instance.Mrak();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
